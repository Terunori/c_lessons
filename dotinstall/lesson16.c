#include <stdio.h>

/*
- ポインタ: アドレスを格納するための変数

- メモリ(記憶領域)上で何が起きているかを知る

  アドレス、値
  int型はアドレス4つぶん必要

- 変数名でなくアドレスを使って値を操作する

*/

int main(void) {

    int a;      // メモリ上で必要な領域の確保
    a = 10;     // その領域に値を入れる

    return 0;
}
